server:
  customMiddleware:
     
 - name: ui5-middleware-http-proxy
      mountPath: /processor
      afterMiddleware: compression
      configuration:
        debug: true
        baseUrl: http://localhost:8080
        path: /processor
        secure: false
        auth:
          user: admin
          pass: admin